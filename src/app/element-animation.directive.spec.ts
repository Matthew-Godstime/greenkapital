import { ElementAnimationDirective } from './element-animation.directive';

describe('ElementAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
